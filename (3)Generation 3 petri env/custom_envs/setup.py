
from setuptools import setup

setup(name='gym_petrinet', # name of the package
	version='0.0.1', # version of this release
	install_requires=['gym','Pandas','Numpy','matplotlib'] # specifies the minimal list of libraries required to run the package correctly
)